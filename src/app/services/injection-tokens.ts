import {InjectionToken} from '@angular/core';

export const BASE_PATH_PROVIDER_SERVICE = new InjectionToken('BASE_PATH_PROVIDER_SERVICE');

export const InjectionTokens = {
  BASE_PATH_PROVIDER_SERVICE
};
